## CODEOWNERS for zkpip/zkpip
# Order matters; the last matching pattern takes precedence.
# Prefer organization teams over personal accounts.

# -- Core JSON Schemas (top-level) --
/schemas/**/*.json                               @zkpip/schemas @zkpip/maintainers

# -- Core package schemas (monorepo path) --
/packages/core/schemas/**/*.json                 @zkpip/schemas @zkpip/maintainers

# -- CLI (optional, if you have a dedicated team) --
/packages/cli/**                                  @zkpip/cli     @zkpip/maintainers

# -- Reusable/CI workflows (grouped) --
/.github/workflows/*.yml                          @zkpip/ci      @zkpip/maintainers

# -- Docs (brand, legal, specs) --
/docs/**                                          @zkpip/docs    @zkpip/maintainers

# -- Default owners for everything else --
*                                                @zkpip/maintainers