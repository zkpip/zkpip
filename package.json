{
  "name": "zkpip",
  "private": true,
  "scripts": {
    "build": "npm -w @zkpip/core run build && npm -w @zkpip/cli run build",
    "test": "npm -w @zkpip/core run test"
  },
  "dependencies": {
    "tsx": "^4.20.5"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "5.5"
  },
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=18"
  },
  "type": "module"
}
