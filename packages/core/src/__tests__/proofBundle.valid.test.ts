// DEPRECATED

// packages/core/src/__tests__/proofBundle.valid.test.ts
import { describe, it } from 'vitest';

describe.skip('ProofBundle — VALID vectors (deprecated, migrating to ProofEnvelope v1)', () => {
  it('deprecated', () => {
    /* intentionally skipped during migration */
  });
});
