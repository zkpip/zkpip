### PLONK smoke (snarkjs)

Generate tiny, reproducible vectors and run the CLI smoke:

```bash
npm -w @zkpip/cli run vectors:plonk:gen
npm -w @zkpip/cli run smoke:plonk
# or
npm -w @zkpip/cli run test:smoke:plonk
```
