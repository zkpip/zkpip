{
  "name": "@zkpip/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "zkpip": "dist/bin.js"
  },
  "main": "dist/bin.js",
  "types": "dist/bin.d.ts",
  "exports": {
    ".": {
      "import": "./dist/bin.js",
      "types": "./dist/bin.d.ts"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "pretest": "npm --prefix ../core install --no-audit --no-fund --ignore-scripts && npm --prefix ../core run build && npm --prefix ../adapters/groth16 install --no-audit --no-fund --ignore-scripts && npm --prefix ../adapters/groth16 run build && npm --prefix ../adapters/plonk install --no-audit --no-fund --ignore-scripts && npm --prefix ../adapters/plonk run build && npm --prefix ../adapters/stark install --no-audit --no-fund --ignore-scripts && npm --prefix ../adapters/stark run build && npm --prefix ../batchseal install --no-audit --no-fund --ignore-scripts && npm --prefix ../batchseal run build && npm run build"
  },
  "dependencies": {
    "@zkpip/adapters-groth16": "file:../adapters/groth16",
    "@zkpip/adapters-plonk": "file:../adapters/plonk",
    "@zkpip/adapters-stark": "file:../adapters/stark",
    "@zkpip/batchseal": "file:../batchseal",
    "@zkpip/core": "file:../core",
    "glob": "^10.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.12",
    "typescript": "^5.5.4",
    "vitest": "^3.2.4"
  }
}
